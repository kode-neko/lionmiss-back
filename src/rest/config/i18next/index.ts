import { en, es } from './locales/index.js';

const i18Config = {
  lng: 'en',
  debug: true,
  resources: {
    en: {
      translation: en,
    },
    es: {
      translation: es,
    }
  }
}

export default i18Config;